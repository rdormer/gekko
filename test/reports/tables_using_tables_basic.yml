# Demonstrates a table using another table as a data set
# Also demonstrates that the tables key for a table is array wrapped

sources:
  gappers:
    csvfile: "test/fixtures/test_data_set_1.csv"

tables:
  source_table:
    sources: gappers
  test_table:
    tables: source_table

output:
  tables:
    - test_table
